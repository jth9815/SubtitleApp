<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_subtitle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_subtitle"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubtitleApp">

        <!-- 런처: 모델 설치 여부 확인 후 분기 -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 모델 다운로드 화면 -->
        <activity
            android:name=".model.ModelSetupActivity"
            android:exported="false"/>

        <!-- 메인 화면 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- 설정 화면 -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"/>

        <!-- 오디오 캡처 서비스 -->
        <service
            android:name=".audio.AudioCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"/>

        <!-- 자막 오버레이 서비스 -->
        <service
            android:name=".overlay.SubtitleOverlayService"
            android:exported="false"/>

    </application>
</manifest>
